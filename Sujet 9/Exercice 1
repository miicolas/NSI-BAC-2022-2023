def calcul(k): # Erreur dans l'énoncé
    liste = [k]
    while k > 1:
        if k%2 == 0:
            k = k // 2
        else:
            k = 3*k + 1
            liste.append(k)
    return liste
