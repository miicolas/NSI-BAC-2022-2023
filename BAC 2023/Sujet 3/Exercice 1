def moyenne(lst):
    sum_coef = 0
    sum_val = 0
    for v, c in lst:
        sum_coef += c
        sum_val += v * c
    if sum_coef == 0:
        return None
    return sum_val / sum_coef


lst = (8, 2), (12, 0), (13.5, 1), (5, 0.5)
print(moyenne(lst))